name: dartpay
description: A real-payment capable Flutter UPI fintech learning app.
publish_to: 'none'
version: 2.0.0+3

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.1
  cupertino_icons: ^1.0.8
  go_router: ^14.6.2
  http: ^1.2.2
  mobile_scanner: ^6.0.2
  provider: ^6.1.2
  razorpay_flutter: ^1.3.7
  share_plus: ^10.1.3
  shared_preferences: ^2.3.3
  upi_pay: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
